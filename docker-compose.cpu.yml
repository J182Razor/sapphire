services:
  sapphire:
    build:
      context: .
      dockerfile: Dockerfile.cpu
    image: sapphire:cpu
    container_name: sapphire
    ports:
      - "8073:8073"
    volumes:
      - ./user:/app/user
    env_file:
      - .env
    restart: unless-stopped
    stdin_open: true
    tty: true
    healthcheck:
      test: ["CMD", "curl", "-kf", "https://localhost:8073/api/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
